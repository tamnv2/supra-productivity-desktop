# NEXT ACTION

Updated: 2026-09-19

## Root cause from test.6

Owner confirmed **Đang lấy hàng and Sản lượng share one Dashboard source/session**.

test.6 incorrectly required two configurations. Runtime evidence showed:
- current-picking request: HTTP 200, parsed 10 active rows;
- production branch: HTTP 200 but `zip: not a valid zip file`;
- User/PDA / Pick / Pack / Phân ca stayed empty.

The production branch had been pointed at the same JSON response and then treated it as an XLSX file.

## test.7 candidate

### One Dashboard configuration
- one cURL/session only;
- derive current-picking request internally;
- derive payroll/productivity XLSX export internally;
- same protected session is applied to both;
- test.6 saved session is automatically migrated on startup.

### Excel processing path
Dashboard payroll XLSX
→ production-row parser
→ enrich employee reference from current-picking data
→ Mapping-equivalent normalized rows
→ Phân ca
→ in-shift/overtime + even/odd classification
→ Pick
→ Pack.

User/PDA is generated by merging production users with current-picking employee/PDA/client fields.

### Regression corrections
- Site filter corrected to 1291.
- KIỂM TRA ĐỒNG BỘ validates both parsers, not only HTTP status.
- valid prior snapshots are retained if one request fails.
- sanitized logs include input/output row counts for every processing stage.

## Immediate next action

Run full PR CI for **v1.3.2-test.7**. Publish only after state guard, public guard, unit tests, vet, Windows x64 build, binary scan and packaging pass.

After publication Owner only needs to update and run:
1. **KIỂM TRA ĐỒNG BỘ**;
2. **ĐỒNG BỘ**;
3. inspect User/PDA, Phân ca, Pick, Pack, Đang lấy hàng.

The same cURL must not be entered twice again.
